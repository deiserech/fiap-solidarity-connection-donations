apiVersion: apps/v1
kind: Deployment
metadata:
  name: solidarity-connection-donations
  namespace: solidarity-connection
spec:
  replicas: 1
  selector:
    matchLabels:
      app: solidarity-connection-donations
  template:
    metadata:
      labels:
        app: solidarity-connection-donations
    spec:
      containers:
        - name: solidarity-connection-donations
          image: solidarityconnectionacr.azurecr.io/solidarity-connection-donations:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80
          readinessProbe:
            httpGet:
              path: /health
              port: 80
            initialDelaySeconds: 90
            periodSeconds: 10
            timeoutSeconds: 10
            failureThreshold: 10
            successThreshold: 1
          livenessProbe:
            httpGet:
              path: /health
              port: 80
            initialDelaySeconds: 90
            periodSeconds: 10
            timeoutSeconds: 10
            failureThreshold: 10
            successThreshold: 1
          resources:
            requests:
              cpu: 50m
              memory: 512Mi
            limits:
              cpu: 200m
              memory: 600Mi
          env:
            - name: ASPNETCORE_ENVIRONMENT
              valueFrom:
                configMapKeyRef:
                  name: donations-api-config
                  key: ASPNETCORE_ENVIRONMENT
            - name: DONATION_TOPIC
              valueFrom:
                configMapKeyRef:
                  name: donations-api-config
                  key: DONATION_TOPIC
            - name: DONATION_SUBSCRIPTION
              valueFrom:
                configMapKeyRef:
                  name: donations-api-config
                  key: DONATION_SUBSCRIPTION
            - name: NewRelic__OtlpEndpoint
              valueFrom:
                configMapKeyRef:
                  name: donations-api-config
                  key: NewRelic__OtlpEndpoint
            - name: NewRelic__Protocol
              valueFrom:
                configMapKeyRef:
                  name: donations-api-config
                  key: NewRelic__Protocol
            - name: ConnectionStrings__DefaultConnection
              valueFrom:
                secretKeyRef:
                  name: donations-secret
                  key: ConnectionStrings__DefaultConnection
            - name: ServiceBus__ConnectionString
              valueFrom:
                secretKeyRef:
                  name: fiap-shared-secret
                  key: ServiceBus__ConnectionString
            - name: NEW_RELIC_LICENSE_KEY
              valueFrom:
                secretKeyRef:
                  name: fiap-shared-secret
                  key: NEW_RELIC_LICENSE_KEY

