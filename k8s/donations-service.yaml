apiVersion: v1
kind: Service
metadata:
  name: solidarity-connection-donations
  namespace: solidarity-connection
  labels:
    app: solidarity-connection-donations
spec:
  type: LoadBalancer
  selector:
    app: solidarity-connection-donations
  ports:
    - name: http
      port: 8082
      targetPort: 80
